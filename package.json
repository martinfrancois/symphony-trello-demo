{
  "name": "symphony-trello-demo-cli",
  "version": "0.1.0",
  "private": true,
  "description": "Disposable demo CLI used to record the Symphony for Trello README walkthrough.",
  "type": "module",
  "bin": {
    "symphony-trello-demo": "src/cli.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "4.19.2",
    "typescript": "5.7.2"
  }
}
