# Agent instructions

This is a small TypeScript CLI. Keep changes focused and readable.

## Checks

Run both before opening a pull request:

```bash
npm run typecheck
npm test
```

## Commits and pull requests

- Use Conventional Commits (for example `fix: ...`, `feat: ...`, `docs: ...`).
- Keep each pull request scoped to one change.
